# Реестр приказов и распоряжений (Order Registry)

Веб-приложение на базе Django 5.1+ для автоматизации учета, хранения и поиска организационно-распорядительных документов (приказов и распоряжений).

Приложение предоставляет интерфейс для работы с реестром документов, поддерживает загрузку скан-копий (PDF), массовый импорт исторических данных из Excel и гибкую систему фильтрации.

## ✨ Ключевые возможности
* Учет документов: Разделение на «Приказы» и «Распоряжения».
* Файловое хранилище: Загрузка скан-копий с автоматическим именованием файлов (формат: тип_номер_дата.pdf) и распределением по папкам год/месяц/.
* Импорт данных: Массовая загрузка реестра из Excel с автоматическим подтягиванием сканов из указанной папки.
* Экспорт: Выгрузка реестра в Excel с возможностью выбора конкретных полей для отчета.
* Поиск и фильтрация:
  * Полнотекстовый поиск по названию.
  * Фильтрация по году, виду документа и точному номеру.
* Безопасность и логирование:
  * Разграничение прав доступа.
  * Подробное логирование действий пользователей (кто что искал, скачивал, менял) и системных ошибок в разные потоки.
* Интерфейс: Адаптивный UI на Bootstrap 5, работа с модальными окнами через AJAX.

## 🛠 Технологический стек
* Python 3.11+
* Django 5.1.4
* PostgreSQL (рекомендуется для продакшена) / SQLite (dev)
* Pandas & OpenPyXL (обработка Excel)
* Bootstrap 5 (Frontend)
* jQuery (AJAX-запросы)

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/yourusername/OrderRegistry.git
cd OrderRegistry
```

### 2. Создание и активация виртуального окружения
```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux / macOS
source venv/bin/activate
```
### 3. Установка зависимостей
```bash
pip install -r requirements.txt
```
### 4. Настройка переменных окружения
Создайте файл `.env` в корне проекта (рядом с `manage.py`), используя пример из `env.sample`:
```bash
cp env.sample .env
```
Откройте `.env` и заполните:
```Python
envSECRET_KEY="очень-длинный-случайный-ключ-генерируйте-новый!"
DEBUG=True

# Разрешённые хосты (через запятую без пробелов)
ALLOWED_HOSTS="127.0.0.1,localhost"

# PostgreSQL (рекомендуется)
DB_ENGINE="django.db.backends.postgresql"
DB_NAME="order_registry_db"
DB_USER="your_postgres_user"
DB_PASSWORD="your_postgres_password"
DB_HOST="localhost"
DB_PORT=5432

# Название вашей организации (отображается в шапке)
ORGANIZATION_NAME="ООО «Рога и Копыта»"
```
#### Критические настройки в `.env`:
* `SECRET_KEY`: Обязательно смените на свой уникальный ключ.
* `ORGANIZATION_NAME`: Название, которое будет отображаться в шапке сайта (например, "ООО «Рога и Копыта»").
* `DB_*`: Настройки подключения к базе данных. Если строки закомментированы, используется SQLite.

Для быстрого старта можно использовать `SQLite` — просто закомментируйте строки с `DB_*` в `.env` или оставьте их пустыми.

### 5. Подготовка базы данных
#### Примените миграции:
```Bash
python manage.py migrate
```

#### ⚠️ ВАЖНО: Создание таблицы кэша
Проект использует кэширование в базе данных (для списка лет в фильтрах и т.д.). 
Таблицу необходимо создать вручную:
```Bash
python manage.py createcachetable
```
Без этого шага приложение выдаст ошибку `relation "orders_cache_table" does not exist`.

### 6. Суперпользователь и запуск
Создайте супер пользователя — это будет администратор системы.
```Bash
python manage.py createsuperuser
```
Запустите сервер
```Bash
python manage.py runserver
```
Откройте в браузере. По умолчанию: ```http://127.0.0.1:8000``` 

## 💻 Команды управления (Management Commands)

В проекте реализовано несколько кастомных команд для автоматизации рутинных задач. 
Они запускаются через `python manage.py <команда>`.

### 1. `load_orders` — Массовый импорт из Excel
Основная команда для переноса базы приказов из Excel-таблицы в приложение с одновременным прикреплением скан-копий.

**Синтаксис:**
```Bash
python manage.py load_orders <путь_к_excel> <путь_к_папке_со_сканами>
```
**Аргументы:**
* `excel_path`: Путь к файлу `.xlsx`.
* `pdf_dir`: Путь к папке, где лежат PDF-файлы сканов.

**Как это работает:**
* Скрипт читает Excel и сопоставляет столбцы с полями модели `Order`.
* Пытается найти файл скана в указанной папке по шаблонам: `Приказ <номер>.pdf`, `<тип> <номер>.pdf` и т.д..
* Если приказ с таким номером уже есть — обновляет его данные. Если нет — создает новый.
* Файлы автоматически копируются в папку `media/orders_scan/YYYY/MM/`.

**Пример:**
```Bash
python manage.py load_orders data/registry_2023.xlsx C:/Archive/Scans2023/
```
### 2. `make_excel_template` — Создание шаблона для импорта
Генерирует пустой Excel-файл с правильными заголовками, который можно заполнить данными и затем загрузить через команду `load_orders`.

**Синтаксис:**
```Bash
python manage.py make_excel_template [--output-dir OUTPUT_DIR]
```
**Опции**:

* `--output-dir`: (необязательно) Папка для сохранения файла. По умолчанию сохраняет в корень проекта.

**Пример**:

```Bash
python manage.py make_excel_template --output-dir ./documents
```
### 3. `create_json` — Конвертация данных (Legacy)
Служебная команда для преобразования специфичного JSON-формата (из старых систем) в формат, пригодный для фикстур или дальнейшей обработки. Также проверяет наличие сканов.

**Синтаксис:**

```Bash
python manage.py create_json <папка_со_сканами>
```
**Логика:**
* Берет данные из `json/output.json` (путь прописан в коде).
* Преобразует даты, проверяет наличие файлов в указанной папке.
* Сохраняет результат в `json/orders2024.json`.

### 4. `make_json` — Генерация JSON-шаблона
Создает файл-пример `order_template.json` с одним объектом. 
Полезно для разработчиков, чтобы понять структуру данных модели.

**Синтаксис:**
```Bash
python manage.py make_json
```
## 📝 Логирование
Система ведет подробные логи в директории `logs/` (создается автоматически).

1. logs/orders_logs.log: Основной файл логов.
   * Ротация происходит ежедневно в полночь.
   * Хранятся логи за последние 30 дней.
2. Уровни логирования:
   * **INFO:** Действия пользователей (Вход, Поиск, Просмотр карточки, Скачивание, Создание/Редактирование).
   * **WARNING/ERROR:** Системные ошибки и сбои (ошибки валидации, проблемы с БД, отсутствующие файлы).

## 🐳 Деплой в Продакшн
Для обеспечения стабильности, безопасности и производительности в продакшн-среде необходимо выполнить ряд шагов. Рекомендуемая конфигурация включает PostgreSQL (БД), Gunicorn (ASGI/WSGI-сервер) и Nginx (фронтенд-сервер/реверс-прокси).

### 1. Настройка Продакшн-окружения
Убедитесь, что ваш файл `.env` настроен для продакшена:

|Параметр|Значение|Описание|
|--------|--------|--------|
|DEBUG|False|Отключает режим отладки для безопасности|
|SECRET_KEY|***|Используйте сильный, уникальный ключ|
|ALLOWED_HOSTS|server_ip, yourdomain.com|Список разрешенных IP-адресов или доменных имен|
|DB_NAME, DB_USER, DB_PASSWORD|PostgreSQL|Настройте боевую базу данных (PostgreSQL или другой промышленный СУБД)|

### 2. Сбор статических и медиа-файлов
Django не должен раздавать статические файлы самостоятельно в продакшене. За это отвечает Nginx. Сначала нужно собрать все статические файлы в одну папку:

```Bash
# Активируйте ваше виртуальное окружение, если еще не сделали этого
source venv/bin/activate

# Собираем все статические файлы проекта в STATIC_ROOT
python manage.py collectstatic
```
Это перенесет все CSS, JS и изображения в папку, указанную в `settings.STATIC_ROOT` (обычно это `/var/www/orders/static`).

### 3. Настройка Gunicorn (ASGI/WSGI-сервер)
Gunicorn будет выступать в роли сервера приложений, который запускает Django и обрабатывает запросы.

#### Установка
```Bash
pip install gunicorn
```
#### Запуск (тест)
Выполните тестовый запуск, чтобы убедиться, что приложение работает:
```Bash
# Обратите внимание: orders_registry - это имя вашего основного проекта Django
gunicorn orders_registry.wsgi:application --bind 0.0.0.0:8000
```
#### Настройка systemd (рекомендуется): Для автоматического запуска и управления Gunicorn создайте файл сервиса orders.service (например, в /etc/systemd/system/):
```Ini, TOML
[Unit]
Description=Gunicorn instance for Order Registry
After=network.target

[Service]
User=www-data # Пользователь, под которым будет работать Gunicorn
WorkingDirectory=/path/to/project/OrderRegistry
Environment="PATH=/path/to/project/OrderRegistry/venv/bin"
ExecStart=/path/to/project/OrderRegistry/venv/bin/gunicorn orders_registry.wsgi:application \
  --workers 3 \
  --bind unix:/run/gunicorn/orders.sock

[Install]
WantedBy=multi-user.target
```
#### Запустите сервис и включите его автозапуск:
```Bash
sudo systemctl daemon-reload
sudo systemctl start orders
sudo systemctl enable orders
```
### 4. Настройка Nginx (Реверс-прокси и статика)
Nginx будет слушать порт 80/443, обрабатывать запросы и перенаправлять их Gunicorn'у, а также самостоятельно отдавать статические и медиа-файлы.

Создайте конфигурационный файл Nginx (например, `/etc/nginx/sites-available/orders_registry.conf`):
```Nginx
server {
    listen 80;
    server_name yourdomain.com; # Укажите ваш домен или IP

    client_max_body_size 50M; # Максимальный размер загружаемого файла (для сканов)

    # 1. Раздача статических файлов (CSS, JS)
    location /static/ {
        alias /path/to/project/OrderRegistry/static/;
    }

    # 2. Раздача медиа-файлов (сканы приказов)
    location /media/ {
        alias /path/to/project/OrderRegistry/media/;
    }

    # 3. Перенаправление всех остальных запросов на Gunicorn
    location / {
        include proxy_params;
        proxy_pass http://unix:/run/gunicorn/orders.sock;
    }
}
```
#### Активация
Создайте символическую ссылку и перезагрузите Nginx:
```Bash
sudo ln -s /etc/nginx/sites-available/orders_registry.conf /etc/nginx/sites-enabled/
sudo nginx -t # Проверка синтаксиса
sudo systemctl restart nginx
```
## 🗄 Структура проекта
Основной код проекта разделен на два логических блока:

| Директория/Файл      |Описание|
|----------------------|--------|
| `orders_registry/ `    |Главный проект Django. Содержит `settings.py`, `urls.py` и настройки ASGI/WSGI|
| `orders/ `             |Основное приложение (`app`). Содержит модели, представления, формы и шаблоны для работы с приказами и распоряжениями|
| `management/commands/` |Кастомные команды Django, такие как `load_orders` и `make_excel_template`|
| `media/`               |Медиа-файлы. Здесь хранятся все загруженные скан-копии приказов (`media/orders_scan/`)|
|`static/`|Статические файлы. Содержит JS, CSS, изображения для фронтенда|
|`logs/`|Логи приложения. Содержит `orders_logs.log` и другие служебные логи|
|`requirements.txt`|Список всех зависимостей Python|

## 🤝 Вклад в проект (Contributing)
Мы приветствуем любые улучшения и дополнения к функционалу.
* Форкните репозиторий.
* Создайте свою ветку (`git checkout -b feature/AmazingFeature`).
* Зафиксируйте свои изменения (`git commit -m 'Add some AmazingFeature'`).
* Отправьте изменения в удаленный репозиторий (`git push origin feature/AmazingFeature`).
* Откройте **Pull Request**.

## 📜 Лицензия
Проект распространяется под лицензией **MIT License**. См. файл `LICENSE` для получения дополнительной информации.